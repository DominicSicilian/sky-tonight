{
  "city": "New York",
  "state": "NY",
  "zip": "",

  "email_to": "you@example.com",
  "delivery": "connector",

  "_comment": "Only city + state are required. zip is optional (refines coordinates). Everything else below is OPTIONAL and auto-derived if omitted: latitude, longitude, timezone, elevation_m, limiting_magnitude. Add any of them here to override the estimate. For SMTP delivery instead of a connector, set delivery to 'smtp' and add email_from/smtp_host/smtp_port (see COWORK_SETUP.md)."
}
